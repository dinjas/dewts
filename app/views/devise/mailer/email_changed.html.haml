%p
  Hello #{@email}!

%p
  We're contacting you to notify you that your email
  - if @resource&.unconfirmed_email
    is being changed to #{@resource.unconfirmed_email}.
  - else
    has been changed to #{@resource.email}.
